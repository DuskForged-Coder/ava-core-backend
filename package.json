{
  "name": "ava-core-backend",
  "version": "1.2.0",
  "private": true,
  "description": "AVA Core hybrid backend with LLM-first orchestration and deterministic retrieval fallback",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/hybrid-assistant.js"
  },
  "engines": {
    "node": ">=20"
  }
}
